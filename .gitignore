# Local tooling — not part of the deployed site
cc-shot.py
csp-test.py

# Stale backups / scratch (git history is the backup now)
index-v1-backup.html
*.bak
index.html.bak-*

# OS / editor cruft
.DS_Store
Thumbs.db
